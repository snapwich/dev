services:
  ssh:
    build:
      context: .
      ssh:
        - default
    ports:
      - "2222:22"
    volumes:
      # DOCKER_SSH_SOCK is the path Docker sees (inside VM on macOS, host path on Linux)
      - ${DOCKER_SSH_SOCK}:/ssh-agent
