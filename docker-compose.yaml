
services:
  ssh:
    build:
      context: .
      args:
        UID: ${DEV_UID}
        GID: ${DEV_GID}
    ports:
      - "2222:22"
    volumes:
      - ~/.ssh:/home/dev/.ssh
      - ~/.gnupg:/home/dev/.gnupg
      - repos:/home/dev/repos
    user: "${DEV_UID}:${DEV_GID}"
volumes:
  repos:
